import { CLICK_SIGN_IN_BUTTON } from "../../Login/Actions/types";

const initialState={};
export const DemoReducer=(state=initialState .action)=>{

    return {...state,fun:{
        lol:123
    }};
}